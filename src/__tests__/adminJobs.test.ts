import { describe, it, expect } from 'vitest'
import AdminJobs from '../components/admin/AdminJobs.vue'

describe('AdminJobs Component', () => {
  it('loads component successfully', () => {
    expect(AdminJobs).toBeDefined()
  })
})
